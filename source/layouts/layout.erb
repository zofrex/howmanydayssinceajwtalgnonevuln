<!doctype html>
<html lang="en">
  <head>
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-12922701-4"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-12922701-4');
    </script>
    <meta charset="utf-8">
    <title><%= h("#{if current_page.data.title then "#{current_page.data.title} || " end}How Many Days Has It Been Since a JWT alg=none Vulnerability?") %></title>
    <%= stylesheet_link_tag "styles" %>
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:creator" content="@zofrex" />
    <meta property="og:title" content="<%= h("#{if current_page.data.title then "#{current_page.data.title} || " end}How Many Days Has It Been Since a JWT alg=none Vulnerability?") %>" />
    <% if content_for?(:description) %>
      <meta property="og:description" content="<%= h(yield_content :description) %>" />
    <% end %>
  </head>
  <body>
    <main>
      <%= yield %>
    </main>
    <footer>
      Out of date? <a href="https://twitter.com/zofrex">@ me on Twitter</a><br/>
      &copy; <%= Time.now.year %>
    </footer>
  </body>
</html>
